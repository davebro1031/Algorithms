/**
 * @param {number[]} prices
 * @return {number}
 */
var minimumCoins = function(prices) {
    let totalCost = prices[0]
    for(let pi = 1; pi <= prices.length; pi++){
        let i = pi-1
        // look into memoization 
    }
};

const prices = [1,10,1,1]
const result = minimumCoins(prices)
console.log(result)